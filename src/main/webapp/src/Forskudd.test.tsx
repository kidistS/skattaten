import React from 'react'
import {render, fireEvent} from '@testing-library/react'

import {Forskudd} from './Forskudd'

describe("Forskudd", () => {
  it("is defined", () => {
    expect(Forskudd).toBeDefined();
  });


});